[package]
name = "ghostty-sys"
description = "q0: libghostty 全量嵌入 API FFI（bindgen，vendored 钉版 a887df42 静态链入）"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
links = "ghostty"
build = "build.rs"

[lib]
doctest = false

[dependencies]

[build-dependencies]
# 与 libghostty-vt-sys 0.2.1 同版 bindgen（对 include/ghostty.h 生成嵌入 API 绑定）。
bindgen = "0.72"
