
== A 打包链（package_app.sh + package_dmg.sh）
  [PASS] A1 打包链全绿（docs/q4-evidence/a-package.log）：.app + DMG + cask 再生
  [PASS] A2 真实签名身份（Apple Development，动态解析）
  [PASS] A3 574 主题随包
  [PASS] A4 spctl 评估如实记录（不通过=预期，无公证）
  [PASS] A5 codesign --verify --deep --strict 通过
  [PASS] A6 cask version=0.1.0（workspace/Info.plist/DMG 文件名单源）
  [PASS] A7 cask sha256 钉 DMG 实物
  [PASS] A8 cask url=file:// 本地 DMG
  [PASS] A9 DMG 不入 git（.gitignore /dist）

== B 本地 tap + file:// DMG 全新安装
  [PASS] B1 brew tap（本地目录）+ install --cask ninja：/Applications/Ninja.app 就位
  [PASS] B2 安装副本验签通过（--deep --strict）
  [PASS] B3 隔离属性实测：file:// DMG 安装**带** com.apple.quarantine（0381;6a958a62;;67D33301-86FE-43F4-8CA4-9F0A07E61715）
  [PASS] B4 cask 安装日志（docs/q4-evidence/b-brew-install.log）
  [PASS] B5 DMG 拖拽布局（Ninja.app + /Applications 链接）

== C 打开即日常终端（虚拟屏；含 D2 去隔离路径取证）
  [PASS] D2 xattr -dr 去隔离生效（本机处理路径）
  [PASS] C1 虚拟屏就绪 displayID=104（frame x=未定位；落窗按 NSScreenNumber 匹配）
  [PASS] C2 安装副本 open 启动（pid=56922，open --env 落虚拟屏）
  [PASS] C3 窗口落在虚拟屏（x=-1040）
  [PASS] C4 resources_dir=Contents/Resources/ghostty（bundle 相对优先于烘入开发路径——本机两者都在，实测优先级）
  [PASS] C5 ODP 缺省生效（odp_applied=true，用户配置已隔离）
  [PASS] C6 终端背景像素 ≈ ODP #282c34（{"px":[300,175],"avg":[40,44,52]}；真渲染，截图 c-terminal.png）
  [PASS] C7 真键盘输入经终端进 shell 执行（touch 产物在；打开即日常终端）
  [PASS] C8 终端网格在（79x25）
  [PASS] C9 零插件红线：无 ADE socket、无插件进程

== D1 默认装副本（带隔离）Gatekeeper 真行为
  [PASS] D1 带隔离副本 open → 未启动（Gatekeeper 拦）
  [NOTE] D1 隔离副本未启动（被拦）但本轮 syspolicyd 无新日志——当日多次评估后缓存，无公证仍拦；见 docs/q4-evidence/d1-blocked.log

== D3 HOMEBREW_CASK_OPTS=--no-quarantine（cask quarantine 语义）
  [PASS] D3 HOMEBREW_CASK_OPTS=--no-quarantine：无隔离属性（本机 brew 5.1.8 的有效开关）
  [PASS] D4 无隔离安装副本直接可开（pid=61035）

== D4 手工隔离副本（模拟他人下载）Gatekeeper 真行为
  [NOTE] D5 手工隔离副本 open → 进程启动了（带 Gatekeeper 警告对话；同 D1 的状态依赖，如实记录）
  [PASS] D5 Gatekeeper 评估介入：Prompt shown + 扫描拒绝（docs/q4-evidence/d4-quarantine-copy.log）
  [PASS] D6 副本去隔离后可开（xattr -dr 路径）
  [PASS] D7 spctl -a -vv：rejected（无 Developer ID/公证，如实记录）

== D3b --no-quarantine CLI 开关对照（brew 5.1.8 已禁用，如实记录）
  [PASS] D3b --no-quarantine CLI 开关已禁用（There is no replacement；d3b-cli-switch.log）
  [PASS] D3b 开关被拒后未安装（app 不在）

== E brew uninstall --cask 无残留（env 重装一份再卸）
  [PASS] E1 /Applications/Ninja.app 移除
  [PASS] E2 Caskroom ninja 目录清除（/opt/homebrew/Caskroom/ninja）
  [PASS] E3 无 ninja 进程残留
  [PASS] E4 tap 语义：卸载 app 不拔 tap（收尾统一拔）

== F q3 回归（docs/q3-evidence/run-e2e.sh；资源解析改动无破坏）
  [PASS] F1 q3 套件 OVERALL: PASS（regression-q3.log）

==== q4 E2E 总结：PASS=37 FAIL=0 ====
OVERALL: PASS
