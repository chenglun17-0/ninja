# Ninja：终端宿主

## 问题

现在的终端软件卡在两个极端：

1. **Orca** 是 Agent 的 IDE，也是终端运行环境。能力全，但太重，内存占用大。
2. **Ghostty** 内存小、渲染快，但几乎没有插件面。点击文件路径只能交给系统文件管理器，社区没法在终端里接自己的查看器。

要的不是轻量版 Orca，也不是「加了插件市场的 Ghostty」。

要的是一个 **终端宿主**：默认像 Ghostty 一样轻；需要时由插件增强；宿主本身只提供最基础的能力。

## 产品定义

Ninja 是一个 **可增强的终端（ADE：Augmentable Desktop Environment）**。

- 身份是终端，不是 IDE，不是 Agent 产品。
- 用户打开它，得到的就是一个终端。没装插件时，内存和行为应对标 Ghostty 一类 GPU 终端。
- 终端核就是 Ghostty：PTY、VT、GPU 绘制、配置、键位全部用 libghostty，不自研引擎。开发方式与 cmux 相同——在 Ghostty 上做应用，而不是再做一遍终端。
- Agent、文件预览、Git、编辑器、工作区，全部是插件。未启用的插件占用必须接近零。
- 开源社区通过稳定、窄的能力面贡献插件。官方不预装一套「完整开发环境」。

一句话：人来这里是为了跑命令；插件只在命令不够用时出现。

## 和 cmux 的关系

cmux 证明了这条路：把 libghostty 当库来用（像应用嵌 WebKit），终端配置和键位直接继承 Ghostty。Ninja 走同一条底座。

差别在产品层。cmux 把浏览器、Agent 通知、工作区、侧栏做成宿主功能。Ninja 不把那些做进宿主。Ninja 要的是 **插件化系统**：进程外、版本化、窄原语。默认仍然只是终端。

## 真正要解决的事

用户的任务不是「拥有插件系统」，而是：

1. 日常开终端时，不要为用不到的 IDE/Agent 付内存。
2. 碰到具体缺口（点路径看文件、接 Agent、看 diff）时，能装一个插件补上，而不用换软件或 fork 终端。
3. 插件作者能依赖一套几年不变的基础能力，而不是宿主内部实现，也不是 Ghostty 的嵌入 API。

成功证据不是插件数量或日活，而是：

- 空载内存与 Ghostty 同量级。
- 空载就是可分发的 macOS 终端：多窗口、标签、分屏，别人装上能当日常终端用。
- 「点击路径 → 终端内看文本/代码」只需安装一个插件，不必改宿主。
- 关掉全部插件后，产品仍是完整终端。

## 最小方案

### 主路径

1. 打开 Ninja = 打开终端。无侧栏、无工作区、无 Agent 面板。
2. 终端识别到可点击对象（文件路径、URL、OSC-8 链接）。
3. 用户点击。
4. 宿主发出一次 **命中事件**，询问是否有插件认领。
   - 有：插件处理（例如新开一个标签预览文件）。
   - 无：走系统默认（和 Ghostty 一样）。
5. 预览用完即关（关标签 / Esc / ⌘W）。终端焦点回去。

文件预览是第一个官方示例插件，只覆盖文本和代码，用来证明能力面够用。它不是核心功能，也不做图片/PDF/目录。

### 宿主只提供这些基础能力

只保留插件无法自己凭空创造、且几乎所有增强都用得上的原语：

| 原语 | 做什么 | 不做什么 |
| --- | --- | --- |
| 终端 | PTY、文本、GPU 绘制、输入、剪贴板、标签/分屏（实现上即 libghostty） | 编辑器、文件树、工作区 |
| 命中 | 路径/URL/OSC-8 的识别与点击分发 | 内置「打开方式」菜单产品 |
| 层 | 插件可要一块覆盖层、侧开层面，或新开一个标签铺满 | 官方布局系统、IDE chrome |
| 输入 | 插件可申请一组快捷键 | 命令面板、快捷键市场 |
| 进程 | 插件可拉起自己的辅助进程 | 内置运行时（Node/Python 常驻） |
| 配置 | 终端配置和键位继承 Ghostty；ninja.toml 只声明启用哪些插件和宿主特有项 | 用宿主语言当用户脚本环境；平行维护一套终端配置 |
| 颜色 | 宿主内置默认色板（One Dark Pro，不可卸基线），插件可推完整色板换当前生效配色 | 内置主题切换 UI、主题市场/主题配置面（换主题只经插件原语） |

原则：能用 escape 协议和命中/层/输入做完的，就不在宿主里做功能。Ghostty 嵌入 API 的语义坑停在宿主适配器，不准漏进插件协议。

### 插件规则

- **默认零插件。** 分发物不含预览、不含 Agent。空载成本由宿主单独负责。
- **启用即拉起（2026-08-29 用户产品决策修订）。** enabled 名单里的插件
  宿主启动即拉起；面板开关 on = 立即拉起、off = 立即杀掉并回收
  （进程/层/主题覆盖/socket 全清）。空载门禁不变——默认零插件时依然零进程、
  零 socket。
- **进程隔离。** 插件崩溃不能带走终端。内存有上限，可在界面里看到、可被用户杀掉。
- **能力面要窄、要稳。** 版本化 ABI/协议。插件依赖「命中、层、输入、进程」，不依赖宿主源码结构，也不依赖 `ghostty.h`。
- **官方不特权。** 文件预览插件和社区插件走同一套 API。官方插件的意义是示范，不是预装套件。
- **发现从仓库开始。** 早期不靠排行榜和应用商店运营。能 git 安装、能本地加载即可。

### 用户控制

- 未装对应插件时，点击路径的行为与普通终端一致，不弹「你需要安装插件」。
- 插件列表能看：已装、已启用、是否在跑、占用内存（菜单「插件…」⌘, 的
  面板：每行 = 名/开关/运行状态（运行中 pid + 内存 MB | 已停止 | 未启用）；
  已装 = 插件目录里能看到的可执行 + ninja.toml 名单；开关即启停——
  on = 启用并当场拉起，off = 立即回收并从 enabled
  名单移除、写回 ninja.toml）。列出本地已装插件不是插件市场。
- 任何插件层都能立刻关掉，焦点回到终端。
- 卸载插件后不留后台进程、不留隐藏窗口。

## 刻意不做

- **不做 IDE。** 不内置文件树、多文件编辑、调试器、项目视图。那是 Orca 变重的原因。
- **不做内置 Agent。** Agent 若有价值，以插件存在。宿主不绑定某家模型，不为 Agent 常驻运行时。
- **不做 cmux 式宿主功能。** 不内置浏览器面板、Agent 编排、工作区、通知环。那些是另一款产品。
- **不做 Electron / 常驻 Node。** 这会直接放弃内存目标。
- **不做「配置即插件」。** WezTerm 式 Lua 配置能改自己的终端，养不出可分发的社区插件。配置是配置，插件是别人能安装的包。
- **不做功能开关堆砌。** 用户要的是装/卸插件，不是在宿主里打开 200 个选项去模拟插件。
- **不做强分发市场。** 排行榜和编辑推荐会把生态变成流量场，也会诱使宿主为「平台增长」加重量。
- **不自研终端引擎。** 也不把 Ghostty 做成重度 fork。钉 commit + 最小补丁；缺的能力先停在宿主适配器。

现有软件为什么不够：

- Ghostty：终端做到位，但点击之后没有分发面，社区只能 fork。
- cmux：同样站在 Ghostty 上，但增强做进了宿主。那不是可装卸的插件生态。
- Kitty kittens / WezTerm Lua：能增强，但是个人脚本或私有协议，不是稳定、可隔离的插件包。
- Zellij：插件在 multiplexer 层，不是 GPU 终端本身。
- Hyper：有插件，但 Electron，内存先输。
- Orca：宿主即产品，能力全开，空载已经很重。

## 风险

1. **插件把宿主拖成 IDE。** 一旦官方为「好用」预装侧栏、Agent、编辑器，内存目标死亡。约束：默认零插件；官方示例不超过证明 API 所需。
2. **能力面太宽。** 宽 API 会让宿主永远不能改内部实现（这也是 Ghostty 拒绝插件的原因）。约束：原语宁缺毋滥；新 API 必须先有第二个独立插件需要它。
3. **隔离太贵。** 每个插件一个 Chromium 式进程会比 Orca 更糟。约束：默认零插件、禁用即退出回收、有硬内存上限、空载不创建插件进程。
4. **社区不来。** 若只有官方预览插件，产品退回 Ghostty。验证应看「非官方插件能否只靠文档完成文件预览等价物」。
5. **嵌进 Ghostty 之后变成 Ghostty 发行版。** 约束：产品面是 ADE 协议，不是 Ghostty 功能清单。嵌入 API 破坏性变更显式升级，不把 Ghostty 语义写进插件 ABI。

## 已确认

- 第一年要做出可分发给别人的 macOS 终端，不是内部演示。
- 标签和分屏属于空载宿主，不是插件。
- 官方预览插件只做文本和代码。
- 空载、点击路径、关掉即轻三项通过之前，仓库不公开。
- **终端核是 libghostty 嵌入（2026-08-30）。** 开发从 Ghostty 基础上做插件系统，不再维护自研引擎。
- **键位和终端配置继承 Ghostty（2026-08-30）。** 用户既有 ghostty 配置直接生效；ninja 只注册 Ghostty 没有的动作（插件面板等）；ninja.toml 只管插件/宿主特有项。
- **颜色是插件原语（2026-08-29）。** 宿主内置 One Dark Pro 为不可卸基线配色，不做内置主题系统；主题切换走插件原语（ADE 协议 `theme.set`）。

## 先验证什么

分发给别人之前，先验证三件事：

1. **空载。** 带标签和分屏的宿主，内存是否仍与 Ghostty 同量级。失败则产品不成立。
2. **第一个插件。** 只通过公开原语，完成「点击路径 → 终端内看文本/代码」。失败说明能力面不对。
3. **关掉即轻。** 启用再禁用预览插件后，内存回到空载。失败说明插件泄漏进了宿主。

通过这三项之前，不讨论 Agent、市场、工作区，也不开源。

实现合同见 [PLAN.md](PLAN.md)。
